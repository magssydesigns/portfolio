# Password required to unlock the portfolio (checked in src/lib/auth/session.ts).
PORTFOLIO_PASSWORD=

# Secret used to sign the session cookie. Generate one with: openssl rand -base64 32
SESSION_SECRET=
